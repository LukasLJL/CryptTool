#include <stdio.h>
#include "driveSelector.h"

int main() {
    printf("AMOS ALA PLAYA\n");

    /* Just for testing purpose, you can ignore that lol
    int numberOfDrives = getNumberOfDrives();
    char test[numberOfDrives];
    getAllDrives(test);
    for (int i = 0; i < numberOfDrives; i++){
        printf("%c\n", test[i]);
    } */
}